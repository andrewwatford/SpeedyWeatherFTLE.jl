var documenterSearchIndex = {"docs":
[{"location":"2d-turbulence/#2D-Turbulence","page":"2D Turbulence","title":"2D Turbulence","text":"using SpeedyWeather, NCDatasets, GeoMakie, CairoMakie\n\n# Set up the spectral grid\nspectral_grid = SpectralGrid(trunc=63, nlayers=1)\n# Initialize the barotropic model with random initial conditions\ninitial_conditions = RandomVelocity(spectral_grid)\n# Initialize the model\nmodel = BarotropicModel(spectral_grid; initial_conditions)\nsimulation = initialize!(model)\n# Run the simulation for 20 days\nrun!(simulation, period=Day(20), output=true)\n\nrun_folder = model.output.run_folder\nfilename = model.output.filename\nds = NCDataset(joinpath(run_folder, filename))\nlons = ds[\"lon\"][:]\nlats = ds[\"lat\"][:]\nvor = ds[\"vor\"][:, :, 1, end]\n\nfig = Figure()\nax = GeoAxis(fig[1, 1], title = \"2D Turbulence: Vorticity after 20 days\")\nsp = surface!(ax, lons, lats, vor; shading = NoShading)\ncb = Colorbar(fig[1, 2], sp; label = \"ω [1/s]\", width = 18, height = Relative(0.7))\n\nfig","category":"section"},{"location":"get_FTLE/#Plotting-the-Finite-Time-Lyapunov-Exponent-(FTLE)","page":"Plotting the FTLE","title":"Plotting the Finite-Time Lyapunov Exponent (FTLE)","text":"In this example we show how to plot the positive- and negative-time Lyapunov exponents (FTLEs) using SpeedyWeatherFTLE.\n\nusing SpeedyWeatherFTLE, RingGrids\n\n# Set up a FullGaussianGrid and u, v fields on it\nnlat_half = 20\nspatial_grid = FullGaussianGrid(nlat_half)\nu = 100 * rand(spatial_grid)\nv = 100 * rand(spatial_grid)\n\npFTLE, p_spectral_grid, p_time_hours = get_FTLE(u, v; dynamics=true, backwards=false) # dynamics=true to evolve the velocity fields\npFTLE_final = Field(pFTLE[:, end], spatial_grid)\nnFTLE, n_spectral_grid, n_time_hours = get_FTLE(u, v; dynamics=true, backwards=true) # to obtain negative-time FTLE\nnFTLE_final = Field(nFTLE[:, end], spatial_grid)\n\np_fig, p_ax, p_sp, p_cb = surface_plot(\n    pFTLE_final;\n    title = \"pFTLE field at $(p_time_hours[end]) hours\",\n    label = \"pFTLE [1/h]\",\n)\np_fig\n\nn_fig, n_ax, n_sp, n_cb = surface_plot(\n    nFTLE_final;\n    title = \"nFTLE field at -$(n_time_hours[end]) hours\",\n    label = \"nFTLE [1/h]\",\n)\nn_fig","category":"section"},{"location":"greet/#Greeting","page":"Greeting","title":"Greeting","text":"This page demonstrates greet().\n\nusing SpeedyWeatherFTLE\n\ngreet()","category":"section"},{"location":"#SpeedyWeatherFTLE","page":"Home","title":"SpeedyWeatherFTLE","text":"Documentation for SpeedyWeatherFTLE.","category":"section"}]
}
